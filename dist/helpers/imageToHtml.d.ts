import { Image } from "../types";
export declare const IMAGE_MAX_WIDTH = 500;
export declare const IMAGE_MIN_WIDTH = 200;
export declare const IMAGE_MAX_HEIGHT = 700;
export declare const IMAGE_MIN_HEIGHT = 300;
export declare const imageToHtml: (image: Image, index: number, timeBetween: number) => HTMLImageElement;
